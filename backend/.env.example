# Server Config
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development

# MongoDB Connection (Local or MongoDB Atlas)
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=weintern_attendance

# Security & JWT
JWT_SECRET_KEY=weintern_super_secret_jwt_key_change_in_production_2026
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Initial Admin Credentials (Auto-created on startup if not present)
DEFAULT_ADMIN_EMAIL=admin@weintern.com
DEFAULT_ADMIN_PASSWORD=admin@weintern123
DEFAULT_ADMIN_NAME="System Administrator"

# AI Thresholds
RECOGNITION_THRESHOLD=0.85
REVIEW_THRESHOLD=0.70
OFFICE_START_TIME="10:00"
